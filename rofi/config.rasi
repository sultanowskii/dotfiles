configuration {
  modi: [ drun, run, ssh, filebrowser, combi ];
  combi-modi: [ drun, run, ssh, filebrowser ];
  timeout {
      action: "kb-cancel";
      delay:  0;
  }
  filebrowser {
      directories-first: true;
      sorting-method:    "name";
  }
}

@theme "squared-nord"
